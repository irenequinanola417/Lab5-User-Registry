Debugging Notes – Lab 6

Bug Encountered:
The program crashed when the user entered text instead of a number for the age.

Diagnosis:
I discovered that converting age using int() caused a ValueError when the user entered letters instead of numbers.

Solution:
I added a try/except block to catch the ValueError and display an error message. The program now asks the user to enter a valid number before continuing.
